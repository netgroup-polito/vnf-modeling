The folders nf-modeling-master and j-verigraph-master contain two java projects.

The first one contains the library developed inside the package definitions, the parser inside the package it.polio.parser and the defined virtual network functions 
inside the package test.definition.

The second one contains the class created with the parser inside the package mcnet.netobjs.NF and the new tests inside the packages myTests and myTests.examples.